[build]
builder = "NIXPACKS"
buildCommand = "pnpm install --no-frozen-lockfile && pnpm --filter @workspace/plonopolis run build"

[deploy]
startCommand = "npx serve -s artifacts/plonopolis/dist/public -l $PORT"
healthcheckPath = "/"
